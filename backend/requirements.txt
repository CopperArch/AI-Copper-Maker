# Version floors = oldest known-good at release time; upper bounds keep
# future majors from breaking the single-file backend silently. To freeze a
# known-good machine exactly: `pip freeze > requirements.lock`.
fastapi>=0.110,<1
uvicorn[standard]>=0.29,<1
httpx>=0.27,<1
python-multipart>=0.0.9,<1
aiofiles>=23.2,<25
pydantic>=2.7,<3
caldav>=1.2,<2
icalendar>=5.0,<7
apscheduler>=3.10,<4
androguard>=3.5,<4
